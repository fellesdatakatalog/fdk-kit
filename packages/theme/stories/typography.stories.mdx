import {
  Meta,
  Title,
  Subtitle,
  Heading,
  Subheading,
  Typeset
} from '@storybook/addon-docs/dist/blocks';

import { version } from '../package.json';

import theme, { Unit } from '../src/theme';
import { createFontsStylesheet } from '../src/styles';

<>{createFontsStylesheet()}</>

<Meta
  title="System/Typography"
  parameters={{ previewTabs: { canvas: { hidden: true } } }}
/>

<Title>Typography</Title>
<Subtitle>
  <small>Version {version}</small>
</Subtitle>
<Subtitle>
  <a href="https://fonts.google.com/specimen/Heebo" target="_blank" style={{ fontSize: 'inherit' }}>
    Heebo
  </a> is the primary brand typeface.
</Subtitle>
<Subheading>Thin ({theme.font.weight.FW100})</Subheading>
<Typeset
  fontFamily='Heebo'
  fontSizes={Object.values(theme.font.size).map(size => size[Unit.PX])}
  fontWeight={theme.font.weight.FW100}
/>
<Subheading>Light ({theme.font.weight.FW300})</Subheading>
<Typeset
  fontFamily='Heebo'
  fontSizes={Object.values(theme.font.size).map(size => size[Unit.PX])}
  fontWeight={theme.font.weight.FW300}
/>
<Subheading>Regular ({theme.font.weight.FW400})</Subheading>
<Typeset
  fontFamily='Heebo'
  fontSizes={Object.values(theme.font.size).map(size => size[Unit.PX])}
  fontWeight={theme.font.weight.FW400}
/>
<Subheading>Medium ({theme.font.weight.FW500})</Subheading>
<Typeset
  fontFamily='Heebo'
  fontSizes={Object.values(theme.font.size).map(size => size[Unit.PX])}
  fontWeight={theme.font.weight.FW500}
/>
<Subheading>Bold ({theme.font.weight.FW700})</Subheading>
<Typeset
  fontFamily='Heebo'
  fontSizes={Object.values(theme.font.size).map(size => size[Unit.PX])}
  fontWeight={theme.font.weight.FW700}
/>
<Subheading>Extra bold ({theme.font.weight.FW800})</Subheading>
<Typeset
  fontFamily='Heebo'
  fontSizes={Object.values(theme.font.size).map(size => size[Unit.PX])}
  fontWeight={theme.font.weight.FW800}
/>
<Subheading>Black ({theme.font.weight.FW900})</Subheading>
<Typeset
  fontFamily='Heebo'
  fontSizes={Object.values(theme.font.size).map(size => size[Unit.PX])}
  fontWeight={theme.font.weight.FW900}
/>
