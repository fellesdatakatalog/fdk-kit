import {
  Meta,
  Title,
  Subtitle,
  Heading,
  Subheading,
  Source,
  Canvas,
  Story,
  ArgsTable,
} from "@storybook/addon-docs";
import Package from "../package.json";

import SideMenu from "../src";

export const Template = (args) => {
  return <SideMenu {...args} />;
};

<Meta title="Components/SideMenu" />

<Title>SideMenu</Title>
<Subtitle>
  <small>Version ${Package.version}</small>
</Subtitle>
<Subtitle>FDK SideMenu component allow users to navigate.</Subtitle>

<Heading>Installation</Heading>
<Source code={`npm i ${Package.name}`} language="bash" />

<Heading>Usage</Heading>
<Source
  code={`import ${SideMenu.displayName} from '${Package.name}';`}
  language="ts"
/>

<Subheading>Default side menu</Subheading>
<Subtitle />

<Canvas>
  <Story
    name="Default SideMenu"
    args={{
      title: "My side menu",
      menuItems: [
        { id: "https://data.norge.no", title: "Page 1", active: true },
        { id: "https://data.norge.no", title: "Page 2" },
        { id: "https://data.norge.no", title: "Page 3" },
      ],
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Heading>Props</Heading>
<ArgsTable of={SideMenu} />
