import {
  Meta,
  Title,
  Subtitle,
  Heading,
  Subheading,
  Source,
  Canvas,
  Story,
  ArgsTable
} from '@storybook/addon-docs/blocks';
import { name, version } from '../package.json';

import SVG, { SVGTypes } from '../src';

<Meta title='Components/SVG' />

<Title>SVG</Title>
<Subtitle>
  <small>Version {version}</small>
</Subtitle>
<Subtitle>FDK SVG component displays inline svg icons.</Subtitle>

<Heading>Installation</Heading>
<Source code={`npm i ${name}`} language='bash' />

<Heading>Usage</Heading>
<Source code={`import ${SVG.displayName} from '${name}';`} language='ts' />

<Subheading>SVG</Subheading>
<Subtitle />

<Canvas>
  <Story name='SVG'>
    <div style={{ padding: '10px' }}>
      <SVG name='quality1' className='quality1-icon' />
    </div>
  </Story>
</Canvas>

<Heading>Props</Heading>
<ArgsTable of={SVG} />
